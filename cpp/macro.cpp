#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <string>
#include <cstring>
#include <map>
#include <set>
#include <queue>
#include <stack>
#include <ctime>
#include <iomanip>
#include <fstream>
#include <sstream>
#include <bitset>
#include <numeric>
#include <cassert>
#include <unordered_map>
#include <unordered_set>
#include <functional>
#include <complex>
#include <random>
#include <chrono>
#include <climits>

using namespace std;

#define ll          long long
#define ull         unsigned long long
#define ulli        unsigned long long int
#define allc(a)     a.begin(), a.end()
#define rallc(a)    a.rbegin(), a.rend()
#define sz(a)       (int)a.size()
#define drop(a)     cout << a << endl; return;
#define pb          push_back
#define pii         pair<int,int>
#define mpr         make_pair
#define ins         insert
#define endl        "\n"
#define F           first
#define S           second

const int           MOD = 1e9 + 7;
const int           MAXN = 1e5 + 5;
const int           INF = 1e9 + 5;
const double        PI = acos(-1.0);

void solve() {
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int t = 1;
    cin >> t;
    
    for (int i = 0; i < t; ++i) {
        solve();
    }
    
    cerr << "\nTime elapsed : " << clock() * 1000.0 / CLOCKS_PER_SEC << " ms\n";
}
